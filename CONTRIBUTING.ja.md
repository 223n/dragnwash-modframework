# コントリビューション

[English](CONTRIBUTING.md)

見に来てくれてありがとうございます。これは前提 Mod で、ほかの人の Mod がこの上で
動いています。なので変更するときの基準は「5 つの Mod が同時に使っても大丈夫か」で、
小さくて地味なプルリクエストがたいてい一番ありがたいです。

参加するみなさんには[行動規範](docs/CODE_OF_CONDUCT.ja.md)を守ってもらいます。
セキュリティの問題はプルリクエストや公開の Issue には書かずに、
[SECURITY.ja.md](SECURITY.ja.md) の窓口から送ってください。

## コードを書かない手伝い

- **壊れたことを報告する。** `BepInEx/LogOutput.log` とゲームのビルド、環境が
  書いてある不具合報告はすごく助かります。ゲームがアップデートされた直後はとくに
  そうです。
  - 新しい Issue には自動でラベル（種類・対象・深刻度）が付きます。バージョンや
    手順、ログが足りない不具合報告には、それをお願いするコメントが 1 回だけ付きます。
  - そのために、タイトルと本文の文章（コードブロック・表・画像は除きます）と、
    貼られたログのエラー行（パスに入ったユーザー名は伏せます）を、TypeSafe AI の
    分類モデルに送っています（[`tools/issue-triage.py`](tools/issue-triage.py)）。
  - それでも、Issue は全部人が読んでいます。
- **自分の Mod でできないことを教える。** このフレームワークができたのは、Mod ごとに
  同じ仕組みを何度も作り直していたからです。フレームワークに手段がなくてゲームを
  直接パッチしているなら、それを書いた Issue がいちばん役に立ちます。
- **ドキュメントを直す。** プレイヤー向けと Mod 作者向けの説明は
  [Wiki](https://github.com/TomXV/dragnwash-modframework/wiki/Home-ja) にあります。
  Wiki は別のリポジトリで、GitHub の Wiki はプルリクエストを受け付けないので、
  どのページのどこが違うのかを Issue に書いてください。設計と調査の記録は、
  こちらの [`docs/`](docs/) に置いてあります。
- **変わった環境で試す。** Steam Deck や SteamOS 以外の Linux、ARM 版 Windows
  などです。動いても動かなくても結果は役に立つので、よければ
  [`docs/GAME_BUILDS.md`](docs/GAME_BUILDS.md) に書き足してください。
- **スポンサーになる。** そうしたくて、余裕があればで大丈夫です。必須ではありませんし、
  スポンサーにならないと見られないものもありません：https://github.com/sponsors/TomXV

## 大きなものに手を付ける前に

新しいライブラリや公開 API の変更、Mod の読み込み方が変わるようなものは、書く前に
まず Issue を立てて相談してください。作業が無駄にならないようにというのもありますが、
[`docs/DESIGN.ja.md`](docs/DESIGN.ja.md) と [`docs/ROADMAP.ja.md`](docs/ROADMAP.ja.md)
に、あなたの考えとは違う筋書きがもうあるかもしれないからです。

小さな修正ならそこまでしなくて大丈夫です。そのまま送ってください。

## 準備

1. .NET SDK を入れて、ゲームに BepInEx 5.4.23.5 を入れます。
2. 自分のゲームのインストール先から参照アセンブリをコピーします。

   ```bash
   pwsh tools/copy-libs.ps1
   ```

   ゲームが既定の Steam ライブラリにないときは `-GamePath` を渡してください。
   コピー先は Git が無視する `libs/` フォルダーです。ここは
   **絶対にコミットしないでください。**
3. 作業するものをビルドします。

   ```bash
   dotnet build src/DragNWash.ModFramework/DragNWash.ModFramework.csproj -c Release
   ```

4. 各プラグインの DLL を `<Game>/BepInEx/plugins/<アセンブリ名>/` に、
   `DragNWash.ModFramework.Preloader.dll` を `<Game>/BepInEx/patchers/` に置きます。

同じ手順をもっと詳しく書いたものが [README.ja.md](README.ja.md) に、ライブラリごとの
役割は
[Playing well with others (wiki)](https://github.com/TomXV/dragnwash-modframework/wiki/Playing-well-with-others-ja)
にあります。

## 守ってほしい決まり

ここに書いてある決まりを破っているプルリクエストは、マージできません。

- **ゲームのファイル、BepInEx のバイナリ、`libs/` の中身は絶対にコミットしない。**
  push とプルリクエストのたびにチェックが走っていて、引っかかればビルドが失敗します。
  このリポジトリを公開していられるのは、この決まりがあるからです。
- **ゲーム由来の素材は [`docs/CONTENT_POLICY.ja.md`](docs/CONTENT_POLICY.ja.md) に
  従う。** 手で作ったものや、別のものに作り変えたものはかまいませんが、ゲームの
  データをそのまま入れるのは駄目です。
- **ゲームのクラスに触れるコードは `internal` のままにする。** Mod から見えるのは
  フレームワーク自身の型だけです。この境目がいちばん大事なところで、そのおかげで
  ゲームが更新されても、合わせて直すのはフレームワークだけで済みます。
- **公開 API の互換性を壊す変更には、メジャーバージョンを上げることと、それなりの
  理由が要る。** その上で動く Mod も一緒に壊れてしまうからです。

## チェック

CI はゲームのファイルが要らないものを走らせていて、手元でも全部走らせられます。

```bash
python tools/check-repo.py        # バージョン、GUID、CHANGELOG、ドキュメントのリンク
python tools/linekeys.py --check  # 行キーがベクターと一致しているか
python tools/check-commits.py     # コミットメッセージに道具の署名が入っていないか
```

Docker があれば、これ全部と CI がやるビルドまで、CI と同じイメージの中で回せます。
手元に Python も .NET SDK も入れなくて大丈夫です。

```bash
docker compose run --rm checks
```

詳しくは [docs/DOCKER.ja.md](docs/DOCKER.ja.md) を見てください。

最初のリストの最後にあるのが**コミットチェッカー**です。この履歴に名前が残るのは、
そのコミットで何を言うかを決めた人だけで、打ち込んだエディターや補助ツール、IDE の
名前は残しません。ビルドが失敗するのは次の 2 つのときです。

- **メッセージ**に、道具の名前が入った `Co-authored-by` の行や "Generated with" の
  フッター、補助ツールのセッションへのリンクがあるとき。人間の共著者なら歓迎です。
- **作者かコミッター**が道具のアカウントのとき。メッセージがどれだけきれいでも
  弾きます。`dependabot[bot]`、`github-actions[bot]`、Web でマージしたときの
  `GitHub <noreply@github.com>` は通りますし、人の名前ならどんな名前でも通ります。
  Claude は実在する人の名前でもあるので、後ろにモデル名や `[bot]` が付いて初めて
  弾きます。

引っかかったら、コミットを直して（直前のものなら `git commit --amend`、作者が
違うなら `--reset-author` も付けて、古いものなら `git rebase -i`）、もう一度 push
してください。

同じチェッカーのもう 1 つのジョブは、どのコミットの作者も GitHub のアカウントに
なっているかを GitHub に聞きます。コミットのメールアドレスが自分のアカウントに
登録してあるか、GitHub の no-reply アドレスでないと、誰が書いたのか GitHub には
分かりません（どちらも github.com/settings/emails で見られます）。直すには、その
アドレスをアカウントに足してチェックをやり直すか、`git config user.email` で
no-reply アドレスにしてから
`git rebase -i --exec "git commit --amend --no-edit --reset-author" <base>` で
コミットを作り直してください。

中核とライブラリはゲームのアセンブリが要るので、CI ではビルドできません。つまり、
動くのを確かめたのは**あなた**です。どのゲームのビルドとどの環境で、何を見て
確かめたのかを、プルリクエストに書いてください。

## バージョンと CHANGELOG

プロジェクトごとにバージョンがあります。変えるときは、`.csproj` の `<Version>`、
コードの中の `Version` 定数、[CHANGELOG.md](CHANGELOG.md) の
`### <名前> <バージョン>` の見出しの 3 つを合わせてください。`tools/check-repo.py`
が見ているのはまさにここです。プリローダーのパッチャーは中核のバージョンに合わせます。

たいていのプルリクエストではバージョンを上げません。上げるのは、ふつうはリリースを
まとめるときです。

## ドキュメント

`docs/` の中のものはどれも 2 か国語（`NAME.md` と `NAME.ja.md`）あって、お互いに
リンクしています。できれば両方直してください。片方しかできないときは、プルリクエスト
にそう書いてもらえればこちらで引き取ります。訳がないからといって修正を止めることは
ありません。

Wiki は別のリポジトリで、プルリクエストは受け付けていません。変更のせいで Wiki の
どこかが古くなるなら、どのページかを書いてください。

## プルリクエストそのもの

- リポジトリをフォークして `main` からブランチを切り、プルリクエストも `main` に向けて
  出してください。1 つのプルリクエストには話題を 1 つだけにします。
- 1 年後に履歴を読んだ人が、何がなぜ変わったのか分かるように書いてください。何を
  書けばいいかはテンプレートが聞いてくれます。
- 本文は日本語でも英語でもかまいません。両方書かなくても大丈夫です。
- 書きかけ（Draft）のプルリクエストも歓迎です。仕上げる前に設計について話し合いたい
  ときもどうぞ。

プルリクエストを送った時点で、その内容がほかのコードと同じく
[MIT ライセンス](LICENSE)で提供されることに同意したことになります。

## 最後にひとつ

これは非公式のファンプロジェクトで、Gator Dragon Games とは関係ありません。
Mod を入れているときにだけ起きる不具合をゲームの開発元に報告したり、ここにあるものの
サポートを開発元にお願いしたりはしないでください。
